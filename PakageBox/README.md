# 코드프로그래머스 - 택배 상자 꺼내기

택배를 쌓을 때 총 층 수와 타겟 택배가 몇 층인지는 구현을 했는데

마지막 층과 타겟 층의 같은 방향과 다른 방향의 예외를 구하지 못해서 많이 헤멨다..

if (total % 2 === target % 2 && nRemain < numRemain) res -= 1; //같은 방향일 때 마지막 층이 타겟 열까지 채우지 못함

if (total % 2 !== target % 2 && nRemain + numRemain <= w) res -= 1; //다른 방향일 때 층이 타겟 열까지 채우지 못함

이 부분이다..

변수가 많아질 수록 알아보기 쉬운 변수명을 만드는 것도 중요하다고 생각이 든다.
